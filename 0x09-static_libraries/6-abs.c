#include "main.h"

/**
 * _abs - calculates the absolute value of an integer
 * @n: integer
 * Return: the absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}

	return (-n);
}
